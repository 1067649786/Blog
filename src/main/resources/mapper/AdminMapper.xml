<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ygy.news.mapper.AdminMapper">
    <resultMap id="BaseResultMap" type="com.ygy.news.entity.Admin">
        <id column="admin_id" jdbcType="BIGINT" property="adminId" />
        <result column="login_name" jdbcType="VARCHAR" property="loginName" />
        <result column="login_password" jdbcType="VARCHAR" property="loginPassword" />
        <result column="admin_nick_name" jdbcType="VARCHAR" property="adminNickName" />
        <result column="locked" jdbcType="TINYINT" property="locked" />
    </resultMap>

    <sql id="Base_Column_List">
      admin_id, login_name, login_password, admin_nick_name, locked
    </sql>

    <select id="login" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from tb_admin_user
        where login_name = #{arg0} AND login_password=#{arg1} AND locked = 0
    </select>

    <select id="selectByPrimaryKey" parameterType="Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from tb_admin_user
        where admin_id=#{adminId}
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.ygy.news.entity.Admin">
        update tb_admin_user
        <set>
            <if test="loginName!=null">
                login_name=#{loginName},
            </if>
            <if test="loginPassword!=null">
                login_password=#{loginPassword},
            </if>
            <if test="adminNickName!=null">
                admin_nick_name=#{adminNickName},
            </if>
            <if test="locked!=null">
                locked=#{locked},
            </if>
        </set>
        where admin_id=#{adminId}
    </update>

</mapper>